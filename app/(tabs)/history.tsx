import { ScrollView, StyleSheet, Text } from "react-native";

export default function TabTwoScreen() {
  return (
    <ScrollView>
      <Text className="font-bold">Đây là một đoạn văn bản bình thường.</Text>
    </ScrollView>
  );
}

const styles = StyleSheet.create({});
